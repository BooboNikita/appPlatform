<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.appplatform.mapper.primary.StoreLinkConfigMapper">
    
    <resultMap id="BaseResultMap" type="com.app.appplatform.entity.StoreLinkConfig">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="device_brand" property="deviceBrand" jdbcType="VARCHAR"/>
        <result column="brand_aliases" property="brandAliases" jdbcType="VARCHAR"/>
        <result column="link_template" property="linkTemplate" jdbcType="VARCHAR"/>
        <result column="enabled" property="enabled" jdbcType="TINYINT"/>
        <result column="sort_order" property="sortOrder" jdbcType="INTEGER"/>
        <result column="remark" property="remark" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>
    
    <sql id="Base_Column_List">
        id, device_brand, brand_aliases, link_template, enabled, sort_order, remark, create_time, update_time
    </sql>
    
    <!-- 查询所有启用的配置 -->
    <select id="findAllEnabled" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM store_link_config
        WHERE enabled = 1
        ORDER BY sort_order ASC, device_brand ASC
    </select>
    
    <!-- 根据设备品牌查询配置 -->
    <select id="findByDeviceBrand" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT 
        <include refid="Base_Column_List" />
        FROM store_link_config
        WHERE device_brand = #{deviceBrand,jdbcType=VARCHAR}
        AND enabled = 1
        LIMIT 1
    </select>
    
    <!-- 根据设备品牌或别名查询配置 -->
    <select id="findByDeviceBrandOrAlias" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT 
        <include refid="Base_Column_List" />
        FROM store_link_config
        WHERE enabled = 1
        AND (
            device_brand = #{deviceBrand,jdbcType=VARCHAR}
            OR (
                brand_aliases IS NOT NULL 
                AND brand_aliases != '' 
                AND FIND_IN_SET(#{deviceBrand,jdbcType=VARCHAR}, brand_aliases) > 0
            )
        )
        ORDER BY 
            CASE WHEN device_brand = #{deviceBrand,jdbcType=VARCHAR} THEN 1 ELSE 2 END,
            sort_order ASC
        LIMIT 1
    </select>
    
    <!-- 查询默认配置 -->
    <select id="findDefaultConfig" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM store_link_config
        WHERE device_brand = 'default'
        AND enabled = 1
        LIMIT 1
    </select>
    
    <!-- 插入配置 -->
    <insert id="insert" parameterType="com.app.appplatform.entity.StoreLinkConfig" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO store_link_config (
            device_brand, brand_aliases, link_template, enabled, sort_order, remark, create_time, update_time
        ) VALUES (
            #{deviceBrand,jdbcType=VARCHAR},
            #{brandAliases,jdbcType=VARCHAR},
            #{linkTemplate,jdbcType=VARCHAR},
            #{enabled,jdbcType=TINYINT},
            #{sortOrder,jdbcType=INTEGER},
            #{remark,jdbcType=VARCHAR},
            #{createTime,jdbcType=TIMESTAMP},
            #{updateTime,jdbcType=TIMESTAMP}
        )
    </insert>
    
    <!-- 更新配置 -->
    <update id="update" parameterType="com.app.appplatform.entity.StoreLinkConfig">
        UPDATE store_link_config
        SET 
            device_brand = #{deviceBrand,jdbcType=VARCHAR},
            brand_aliases = #{brandAliases,jdbcType=VARCHAR},
            link_template = #{linkTemplate,jdbcType=VARCHAR},
            enabled = #{enabled,jdbcType=TINYINT},
            sort_order = #{sortOrder,jdbcType=INTEGER},
            remark = #{remark,jdbcType=VARCHAR},
            update_time = #{updateTime,jdbcType=TIMESTAMP}
        WHERE id = #{id,jdbcType=BIGINT}
    </update>
    
    <!-- 删除配置 -->
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM store_link_config WHERE id = #{id,jdbcType=BIGINT}
    </delete>
    
    <!-- 启用/禁用配置 -->
    <update id="updateEnabled">
        UPDATE store_link_config
        SET enabled = #{enabled,jdbcType=TINYINT},
            update_time = NOW()
        WHERE id = #{id,jdbcType=BIGINT}
    </update>
    
</mapper>
